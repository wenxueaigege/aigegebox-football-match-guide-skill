# Skill path migrated

The canonical Skill directory is now [`../aigegebox-football-match-guide/`](../aigegebox-football-match-guide/).

This legacy path is kept only as a migration marker for users coming from `1.0.1`. Do not install it as a Skill and do not add a second copy to a discovery directory.
